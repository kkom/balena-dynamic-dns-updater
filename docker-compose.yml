version: '2'
volumes:
    ddclient-config:
services:
  ddclient:
    build: ./ddclient
    volumes:
      - "ddclient-config:/etc/ddclient"
    depends_on:
      - configure
  configure:
    build: ./configure
    volumes:
      - "ddclient-config:/config"
